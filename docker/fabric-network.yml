version: '3.8'

services:
  blockchain-node-1:
    image: hyperledger/fabric-peer:2.5
    container_name: peer0.org1.example.com
    environment:
      CORE_VM_ENDPOINT: unix:///host/var/run/docker.sock
      CORE_PEER_ID: peer0.org1.example.com
      CORE_PEER_ADDRESS: peer0.org1.example.com:7051
      CORE_PEER_LISTENADDRESS: 0.0.0.0:7051
      CORE_PEER_CHAINCODEADDRESS: peer0.org1.example.com:7052
      CORE_PEER_CHAINCODELISTENADDRESS: 0.0.0.0:7052
      CORE_PEER_LOCALMSPID: Org1MSP
      CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE: fabric-network
    volumes:
      - /var/run/docker.sock:/host/var/run/docker.sock
    ports:
      - "7051:7051"
    networks:
      - fabric-network

  orderer:
    image: hyperledger/fabric-orderer:2.5
    container_name: orderer.example.com
    environment:
      ORDERER_GENERAL_LISTENADDRESS: 0.0.0.0
      ORDERER_GENERAL_LISTENPORT: 7050
      ORDERER_GENERAL_GENESISMETHOD: file
      ORDERER_GENERAL_GENESISFILE: /var/hyperledger/orderer/orderer.genesis.block
      ORDERER_GENERAL_LOCALMSPID: OrdererMSP
    volumes:
      - ./config/orderer.genesis.block:/var/hyperledger/orderer/orderer.genesis.block
    ports:
      - "7050:7050"
    networks:
      - fabric-network

  ca:
    image: hyperledger/fabric-ca:1.5
    container_name: ca.org1.example.com
    environment:
      FABRIC_CA_HOME: /etc/hyperledger/fabric-ca-server
      FABRIC_CA_SERVER_CA_NAME: ca-org1
      FABRIC_CA_SERVER_PORT: 7054
    volumes:
      - ./config/ca-server-config.yaml:/etc/hyperledger/fabric-ca-server/fabric-ca-server-config.yaml
    ports:
      - "7054:7054"
    networks:
      - fabric-network
    command: sh -c 'fabric-ca-server start -b admin:adminpw'

networks:
  fabric-network:
    driver: bridge
